# 🪵 Go Logger — лёгкий логгер с уровнями и ротацией файлов

![Go Version](https://img.shields.io/badge/Go-1.23%2B-blue)
![License](https://img.shields.io/badge/License-MIT-green)

Пакет логирования для Go-приложений:

- уровни: **Debug / Info / Warn / Error**
- вывод: **консоль**, **файл** или **оба**
- **ротация файлов** по размеру
- **потокобезопасный** (mutex)
- форматирование как в `fmt.Printf`

---

## ✨ Особенности

- 🎯 Несколько уровней логирования
- 📁 Гибкие режимы вывода (console / file / both)
- 🕒 **Файлы логов создаются сразу с timestamp в имени** (с секундами)
- 🔄 Ротация логов по достижению `maxFileSize` (**только по размеру**)
- ♾️ **Нет лимита по количеству файлов** (архивы не удаляются)
- 📁 **Автосоздание директорий** под путь лога
- 🔒 Потокобезопасность
- 🧩 Минимальная интеграция в CLI/сервис

---

## 📦 Установка

```bash
go get github.com/ZeRg0912/logger@latest
```

---

## 🚀 Быстрый старт

### Инициализация

```go
// Только консоль
_ = logger.InitConsoleOnly(logger.LevelInfo)

// Только файл (с ротацией)
_ = logger.InitFileOnly(logger.LevelDebug, "logs/app.log", 10*1024*1024)

// Консоль + файл
_ = logger.InitBoth(logger.LevelInfo, logger.LevelDebug, "logs/app.log", 10*1024*1024)
```

### Важно: закрывайте логгер, если включён вывод в файл

Если используется `FileOnly` или `Both`, нужно закрывать файл при завершении процесса:

```go
defer logger.Close()
```

Это гарантирует:
- корректное закрытие дескриптора файла (важно на Windows)
- корректное завершение записи

---

## 🧭 Уровни и фильтрация

Логгер печатает сообщение, если уровень сообщения **не ниже** установленного уровня вывода.

- `consoleLevel` влияет на вывод в консоль
- `fileLevel` влияет на запись в файл

Пример:
- `consoleLevel=INFO` → `Debug` в консоль не пойдёт
- `fileLevel=DEBUG` → `Debug` в файл пойдёт

---

## 📚 API

### Обычные сообщения (логируются)

```go
logger.Debug("Отладка: %v", data)
logger.Info("Старт: %s", name)
logger.Warn("Подозрительно: %d", n)
logger.Error("Ошибка: %v", err)
```

### Специальные консольные сообщения

```go
// Всегда показываются в консоли + логируются
logger.ConsoleError("Ошибка для пользователя")
logger.ConsoleInfo("Информация для пользователя")
logger.ConsoleSuccess("Успех операции")

// Только в консоль (НЕ логируются)
logger.ConsoleHelp("Справка по командам")
logger.ConsoleHelpf("Форматированная справка: %s", "команда")
```

---

## 🕒 Имена файлов и 🔄 Ротация по размеру

### **Базовый путь - это шаблон**

Параметр `filePath` (например `logs/app.log`) **не является "текущим файлом"**.
Это **шаблон/база*, на основе которой создаются реальные файлы логов с timestamp.

Пример:
- задано `logs/app.log`
- фактически будет создано (пример): 
`logs/app_02.02.2026_23-10-15.log`

### **Формат timestamp**

В Windows нельзя использовать **:** в имени файла, потому формат такой:
- dd.mm.yyyy_hh-mm-ss
Пример: 02.02.2026_23-10-15

### **Коллизии имен**

Если по какой-то причине имя занято (например, несколько запусков в одну секунду), добавляется суффикс:
- `logs/app_02.02.2026_23-10-15_01.log`
- `logs/app_02.02.2026_23-10-15_02.log`

### **Когда происходит ротация**

Ротация срабатывает когда:
**текущий размер файла + размер следующей записи** превышают `maxFileSize`

Что происходит при ротации:
- текущий файл закрывается
- открывается **новый** timestamp-файл
- старые файлы **не удаляются** (нет лимита по кол-ву)

### **Директории создаются автоматически**

Если указано `logs/app.log`, директория `logs/` будет создана автоматически при инициализации логгера (для `FileOnly`/`Both`).

---

## 🧵 Потокобезопасность и производительность

- Все операции логирования защищены мьютексом.
- Для очень высокочастотного логирования (десятки/сотни тысяч сообщений/сек) mutex может стать узким местом — тогда лучше:
  - уменьшать уровень (`Info` вместо `Debug`)
  - логировать реже
  - писать агрегированные метрики

---

## 🧩 Примеры интеграции

### CLI-приложение (паттерн `realMain()` + гарантированный `Close()`)

```go
func main() {
    os.Exit(realMain())
}

func realMain() int {
    if err := logger.InitBoth(logger.LevelInfo, logger.LevelDebug, "logs/app.log", 10*1024*1024); err != nil {
        fmt.Fprintln(os.Stderr, err)
        return 2
    }
    defer logger.Close()

    logger.Info("Запуск")

    // ... работа

    logger.Info("Завершение")
    return 0
}
```

### Сервис/HTTP

```go
func main() {
    _ = logger.InitConsoleOnly(logger.LevelInfo)

    http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
        logger.Info("%s %s", r.Method, r.URL.Path)
        w.WriteHeader(http.StatusOK)
    })

    logger.Info("listen :8080")
    _ = http.ListenAndServe(":8080", nil)
}
```

---

## 🧯 Частые проблемы

### «Файл не создаётся»
- режим должен быть `FileOnly` или `Both`
- путь должен быть доступен для записи
- смотри ошибку, возвращаемую `Init*` (не игнорируй её в реальном коде)

### «Debug не вижу»
- проверь, что `consoleLevel`/`fileLevel` позволяют `Debug`

### «Файл занят/не могу удалить на Windows»
- убедись что в CLI ты вызываешь `defer logger.Close()` (или `logger.Close()` при заверщении)

---

## 📄 Лицензия

MIT (см. `LICENSE`).

---

## 👨‍💻 Автор

ZeRg0912 — Сергей Полевой
