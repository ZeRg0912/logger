# 🪵 Go Logger - Гибкая система логирования для Go

![Go Version](https://img.shields.io/badge/Go-1.23%2B-blue)
![License](https://img.shields.io/badge/License-MIT-green)
![Version](https://img.shields.io/badge/Version-1.0.0-orange)

Простой, но мощный пакет для логирования в Go-приложениях с поддержкой уровней логирования, ротации файлов и цветового вывода.

---

## ✨ Особенности

- 🎯 **Несколько уровней логирования**: Debug, Info, Warn, Error
- 📁 **Гибкие выходные режимы**: консоль, файл или оба
- 🔄 **Ротация логов** при достижении максимального размера
- 🎨 **Цветовой вывод** в консоль с эмодзи
- 🔒 **Потокобезопасность** благодаря мьютексам
- 📝 **Форматированные сообщения** как в `fmt.Printf`

---

## 🚀 Быстрый старт
### Установка
```
go get github.com/ZeRg0912/logger@latest
```

---

## 📚 Использование
### Инициализация
```
// Только консоль
logger.InitConsoleOnly(logger.LevelInfo)

// Только файл
logger.InitFileOnly(logger.LevelDebug, "logs/app.log", 10*1024*1024)

// Консоль и файл
logger.InitBoth(logger.LevelInfo, logger.LevelDebug, "logs/app.log", 10*1024*1024)
```

### Уровни логирования
```
logger.Debug("Отладочная информация: %v", data)
logger.Info("Информационное сообщение")
logger.Warn("Предупреждение: %s", "низкая память")
logger.Error("Критическая ошибка")
```

### Специальные консольные сообщения
```
// Всегда показываются в консоли + логируются
logger.ConsoleError("Ошибка для пользователя")
logger.ConsoleInfo("Информация для пользователя") 
logger.ConsoleSuccess("Успех операции")

// Только в консоль (не логируются)
logger.ConsoleHelp("Справка по командам")
logger.ConsoleHelpf("Форматированная справка: %s", "команда")
```

### Простое использование
```
package main

import (
    "github.com/ZeRg0912/logger"
)

func main() {
    // Логирование только в консоль
    logger.InitConsoleOnly(logger.LevelInfo)
    
    logger.Info("Приложение запущено")
    logger.Debug("Отладочная информация")
    logger.Error("Ошибка соединения: %s", "таймаут")
    
    // Специальные консольные сообщения
    logger.ConsoleSuccess("Операция завершена!")
    logger.ConsoleError("Что-то пошло не так")
}
```

---

## ⚙️ Конфигурация
### Уровни логирования
| Уровень |         	Назначение|
|---------|             ----------|
|```LevelDebug```|	    Детальная отладочная информация|
|```LevelInfo```|	    Общие операционные сообщения|
|```LevelWarn```|	    Предупреждающие условия|
|```LevelError```|	    Условия ошибок|

### Режимы вывода
|Режим |	            Назначение|
|---------|             ----------|
|```ConsoleOnly```|	    Только консольный вывод|
|```FileOnly```|	    Только запись в файл|
|```Both```|	        Консоль + файл|

---

## 🔧 Примеры
### Полная настройка с ротацией файлов
```
package main

import (
    "github.com/ZeRg0912/logger"
)

func main() {
    // Инициализация с ротацией при 10 МБ
    err := logger.InitBoth(
        logger.LevelInfo,       // Уровень для консоли
        logger.LevelDebug,      // Уровень для файла
        "logs/app.log",         // Путь к файлу
        10*1024*1024,           // Макс. размер: 10 МБ
    )
    
    if err != nil {
        panic(err)
    }
    
    // Использование
    logger.Info("Приложение инициализировано")
    logger.Debug("Параметры: %+v", config)
    
    defer logger.ConsoleSuccess("Приложение завершено")
}
```
### Использование в веб-приложении
```
package main

import (
    "net/http"
    "github.com/ZeRg0912/logger"
)

func main() {
    logger.InitConsoleOnly(logger.LevelInfo)
    
    http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
        logger.Info("Запрос: %s %s", r.Method, r.URL.Path)
        // ... обработка запроса
    })
    
    logger.Info("Сервер запущен на :8080")
    http.ListenAndServe(":8080", nil)
}
```

---

## 📁 Структура лог файла
При использовании файлового вывода логи включают:
```
INFO: 2024/01/15 10:30:25 main.go:15: Приложение запущено
ERROR: 2024/01/15 10:30:26 handler.go:42: Ошибка базы данных: connection refused
```

---

## 🔄 Ротация файлов
Логгер автоматически ротирует файлы при достижении maxFileSize:
```
app.log      # Текущий файл
app_1.log    # Предыдущий (самый новый)
app_2.log    # Более старый
app_3.log    # Еще более старый
app_4.log    # Самый старый
app_5.log    # Удаляется
```

## 📄 Лицензия
Распространяется под лицензией MIT. Смотрите файл LICENSE для деталей.

---

## 👨‍💻 Автор
ZeRg0912 - Сергей Полевой
